<h1><%= @page.title %></h1>
<p><%= @page.body %></p>
<%= link_to 'Back', admin_pages_path %>

<h2>Delete comments:</h2>
<div class="comments">
  <% @comments.each do |form| %>
    <h2><%= form.author %>: <%= form.content %></h2>
      <%= button_to 'delete', [@page, form], method: :delete %>
  <% end %>
</div>
